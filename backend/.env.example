# Render backend env
# Comma-separated origins. Add your Vercel domain in Render env dashboard.
ALLOWED_ORIGINS=http://localhost:5173,https://your-frontend.vercel.app

# Keep disabled on free Render to avoid sentence-transformers OOM on boot.
ENABLE_VECTOR_MEMORY=false

# Required keys
GROQ_API_KEY=
